import {Button, Text} from 'native-base';
import React from 'react';

const ViewDrawVideo = () => {
  return (
    <Button marginBottom={2} colorScheme="blue">
      <Text allowFontScaling={false} color="white" fontWeight="bold">
        View Draw Video
      </Text>
    </Button>
  );
};

export default ViewDrawVideo;
